# URL del proyecto. Supabase → Project Settings → Data API
SUPABASE_URL=

# Llave secreta (equivale a service_role). Solo servidor, nunca en el navegador.
# Supabase → Project Settings → API Keys
SUPABASE_SECRET_KEY=